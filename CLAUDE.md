# MeowZing — กติกาของโปรเจกต์

## เพิ่มของกิน / ไอเท็ม / สิ่งกีดขวางใหม่ → ต้องเพิ่มในหน้าออกแบบด่าน (`editor.html`) ด้วยเสมอ

หน้าออกแบบด่านต้องวางของได้ครบทุกชนิดที่เกมมี ของใหม่ที่เข้าเกมแต่ไม่เข้าหน้านี้ = งานยังไม่เสร็จ

- **ไอเท็มตัวช่วย** (แบบสปีด ตัวโต แม่เหล็ก โล่ ขวดพลัง ตัวอักษร)
  1. `PICKUPS` ใน `src/level.js` — รูปร่างตอนเกิด + array ใน `Level` ที่มันไปอยู่
  2. `ITEM_DEFS` ใน `src/editor/main.js` — ชื่อ คำอธิบาย ภาพ ระยะเก็บ ผล (ชิปในกล่องเครื่องมือสร้างจากตารางนี้เอง)
  3. ถ้าผลของมันเปลี่ยนการเล่น (ชน / หลุม / การเก็บของ) ให้สอน `simulate()` ในหน้าออกแบบด้วย
- **ของกิน**
  - หน้าตาหรือคะแนนใหม่บนเม็ดเดิม → เพิ่มตัวเลือกใน `TOPS` (+ ตัวช่วยใน `AUTHOR` ถ้าต้องใช้)
  - รูปแถวใหม่ → ตัวช่วยใน `AUTHOR` แล้วเพิ่มชิปใน `KIT` + `build()` + `toCode()`
- **ทางเข้าด่าน (Transition)** → ข้อมูล + ท่อนใน `GATES` (`src/gates.js`), ภาพใน `src/render/gates/` แล้วใส่ `gate` ใน `stages.js` — หน้าออกแบบอ่าน `GATE_LIST` เอง (เลือก "ใช้เป็นทางเข้าด่าน") ถ้ามีชุดตั้งต้นให้เพิ่มใน `gateStarter()` ด้วย
- **สิ่งกีดขวาง / ของอันตรายที่ขยับได้** → `KIT` + `build()` + `toCode()` และถ้าขยับเองให้เพิ่มใน `stepMovers()` ด้วยสูตรเดียวกับ `Level`
- **พื้นเหยียบได้** (เนิน / พื้นลอย) → ท่อนคืนคีย์ `plats: [...]` รูปผิวมาจาก `platTop()` ใน `src/level.js` ตัวเดียวกับที่ `footing()` ใช้หาที่ยืน วาดด้วย `drawPlats()` ใน `src/render/platforms.js` — ผิวชนิดใหม่ต้องเพิ่มที่ `platTop()` ที่เดียว ทั้งฟิสิกส์ ทั้งภาพ และหน้าออกแบบจะตามไปเอง

หลักของหน้าออกแบบ: ห้ามมีสูตรของตัวเอง — ตำแหน่ง ขนาด ระยะเก็บ การเคลื่อนที่ ต้องดึงจาก `config.js` / `level.js` ชุดเดียวกับเกม

## ปุ่มปิด (กากบาท) → ใช้เทมเพลต `.xbtn` เดียวกันทุกที่
- หน้าตาต้นแบบคือปุ่มปิดตอนกดขยายเต็มจอในหน้าระบายสี (วงกลมม่วงไล่สี ขอบครีม เงาใต้ปุ่ม กดแล้วยุบ) — กฎอยู่ร่วมกับ `.backbtn` ใน `src/style.css`
- ปุ่มปิดใหม่: `<button type="button" class="xbtn ชื่อของตัวเอง" aria-label="ปิด">` + ไอคอน SVG ตัวเดียวกับ `CLOSE_ICON` ใน `src/main.js` (เส้นหนา 3 ปลายมน)
- คลาสของตัวเองแก้ได้แค่ตำแหน่ง/z-index ห้ามตั้งสี ขอบ เงา ขนาดใหม่
- ปุ่มข้อความ "ปิด"/"ตกลง" แบบ `.btn` ไม่นับ — กฎนี้สำหรับปุ่มไอคอนกากบาทเท่านั้น
