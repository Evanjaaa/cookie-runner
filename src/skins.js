// src/skins.js
// ─────────────────────────────────────────────────────────────
// ชุดสีของแมวแต่ละตัว — ตัวละครวาดด้วยโค้ดชุดเดียวกันทั้งหมด
// เปลี่ยนแค่จานสีกับสวิตช์ลาย/แก้ม ไม่ได้วาดตัวใหม่แยกไฟล์
// อยากเพิ่มแมวตัวที่สาม แค่เพิ่มอ็อบเจกต์ในอาเรย์นี้ เมนูจะขึ้นให้เอง
//
// ── eye กับ ink แยกกัน ──
// เคยเป็นช่องเดียวกันชื่อ ink แล้วมันวาดทั้งดวงตาและเส้นปาก ผลคือแมวตาสีฟ้าได้ปากสีฟ้า
// แยกออกเป็นสองช่องแล้ว: eye = ดวงตา, ink = เส้นปากกับเส้นบนหน้า
//
// ทุกสีต้องอ่านออกบนพื้นหลังม่วงเข้ม (#3A1D50) เป็นข้อบังคับ
// นั่นคือเหตุผลที่แมวขาวใช้ #F6F1FA ไม่ใช่ขาวล้วน และขาใช้เทาอมม่วง
// ไม่ใช่เทากลาง ๆ ซึ่งจะจมหายไปกับฉากหลัง
// ─────────────────────────────────────────────────────────────
import { loadSkin, saveSkin, loadSkinsOwned, saveSkinsOwned } from './storage.js';
import { getOutfit } from './outfits.js';
import { CUSTOM_ID, customSkin } from './paint.js';

// ── จานสีของแมวแต่ละตัว ────────────────────────────────────
//
// ทุกตัวถูกดันให้สดและสว่างขึ้นกว่าชุดเดิม เพราะชุดเดิมเป็นสีธรรมชาติแบบแมวจริง
// (น้ำตาลหม่น เทากลาง ครีม) ซึ่งจมหายไปกับฉากที่สีจัดของเกมนี้
//
// ── ข้อจำกัดที่วัดมาแล้ว อ่านก่อนแก้สี ──
// ฉากในเกมไล่ตั้งแต่เกือบดำ (#07060F อวกาศ) ถึงเกือบขาว (#EAF4FA ทุ่งหิมะ)
// วัดอัตราส่วนความสว่างแบบ WCAG ของตัวแมวเทียบกับฟ้าของทั้งหกฉากแล้วพบว่า
// "ไม่มีสีเรียบสีไหนอ่านออกได้ครบทุกฉาก" — สีที่รอดทั้งสองปลายต้องมีความสว่าง
// ราว 0.18 พอดี ซึ่งเป็นสีค่อนข้างเข้ม ขัดกับตัวตนของขาวมุกกับวิเชียรมาศโดยตรง
//
// เพราะฉะนั้นเรื่องอ่านออกไม่ออกต้องแก้ด้วย "เส้นขอบ" ไม่ใช่ด้วยการเลือกสีตัว
// จานสีชุดนี้จึงเลือกเพื่อบุคลิกกับความจำง่ายเป็นหลัก ยกเว้นดำสนิทที่ต้องดันให้
// สว่างขึ้นจริง ๆ เพราะตัวเข้มบนฉากเข้มไม่มีเส้นขอบสีไหนช่วยได้
//
// ── ช่อง line คือเส้นขอบนั้น ──
// ต้องเข้มกว่าตัวเยอะ ๆ ทุกตัว ไม่ใช่แค่ "เข้มกว่านิดหน่อย" เพราะฉากที่ทำให้
// แมวหายคือฉากสว่าง และฉากสว่างที่ร้ายที่สุดคือชายหาด (#E86A5C) ซึ่งเป็นส้มกลาง ๆ
// เส้นขอบโทนกลางจะจมไปกับมันพอดี — เคยลองแล้ววิเชียรมาศได้ 1.00 คือสีเดียวกันเป๊ะ
// ชุดที่ใช้อยู่นี้วัดแล้วแย่สุด 1.95 (ขาวมุกบนชายหาด) และตัดกับขนของตัวเองอย่างน้อย 1.9
export const SKINS = [
  {
    id: 'orange',
    name: 'ส้มน้อย',
    note: 'จอมซนขนส้ม',
    // ส้มสดขึ้นกว่าเดิมชัดเจน ให้อ่านเป็น "ส้มจี๊ด" ไม่ใช่ส้มอิฐ
    cat: '#FF9538',
    line: '#8A3708',   // ส้มไหม้
    dark: '#D95F14',
    cream: '#FFF1D4',
    pink: '#FF8FAE',
    eye: '#3E1C06',   // สีดวงตา
    ink: '#3E1C06',
    whisker: 'rgba(255,246,232,.8)',
    // เม็ดที่โปรยลงมาตอนใช้ความสามารถ [สีหลัก, ไฮไลต์, ขอบเข้ม]
    rain: ['#FF8A3D', '#FFD9A8', '#C2450F'],
    stripes: true,    // ลายบนหลังกับหน้าผาก
    blush: false,
  },
  {
    id: 'snow',
    name: 'ขาวมุก',
    note: 'ขนปุยนุ่มนิ่ม',
    // ขาวอมม่วงอ่อน ไม่ใช่ขาวเทา — ม่วงจาง ๆ ทำให้ยังเป็นขาวมุกอยู่
    // แต่มีสีในตัวพอให้ไม่กลายเป็นรูตัดกระดาษบนฉากขาว
    cat: '#F7F0FF',
    line: '#6B54A0',   // ม่วงเข้ม เข้าชุดกับขาวอมม่วงของตัว
    dark: '#B9A4E2',
    cream: '#FFFFFF',
    pink: '#FFA6C8',
    eye: '#3A2B58',   // สีดวงตา
    ink: '#3A2B58',
    // ม่วงกลาง ๆ — เข้มพอให้เห็นบนหน้าขาว และสว่างพอให้เห็นบนฉากม่วงเข้ม
    whisker: 'rgba(150,127,196,.92)',
    rain: ['#EFE8FF', '#FFFFFF', '#9B86CE'],
    // ไม่มีลาย แต่มีแก้มชมพูแทน — ทำให้แยกออกจากตัวส้มได้ทันที
    // แม้ตอนวิ่งเร็ว ๆ ที่มองเห็นแค่เงาร่าง
    stripes: false,
    blush: true,
  },
  {
    id: 'midnight',
    name: 'ดำสนิท',
    note: 'เงาในคืนเดือนมืด',
    // ── ตัวเดียวที่เปลี่ยนสีเพราะ "วัดแล้วว่าอ่านไม่ออก" ไม่ใช่เพราะความสวย ──
    // ของเดิม #3B3350 เทียบกับฟ้าถ้ำได้ 1.21 และฟ้าครัวกลางคืนได้ 1.21
    // (1.0 = สีเดียวกันเป๊ะ) แปลว่าบนสองฉากนั้นแมวหายไปกับพื้นหลังจริง ๆ
    // ไม่ใช่แค่ดูยาก ดันขึ้นเป็นม่วงคราม #5A4899 แล้ววัดใหม่ได้ 1.89 ทั้งคู่
    //
    // ยังไม่ใช้ดำจริงเหมือนเดิมด้วยเหตุผลเดิม: ฉากถ้ำกับอวกาศเข้มมาก
    // ถ้าใช้ #000 ตัวแมวจะเป็นเงาทึบ มองไม่ออกว่าหันหน้าทางไหนหรือหมอบอยู่
    cat: '#5A4899',
    line: '#241A45',   // ม่วงคราม เข้มกว่าตัวสองขั้น
    dark: '#9A85E0',
    cream: '#EDE6FF',
    pink: '#FF8FC0',
    eye: '#170F2C',   // สีดวงตา
    ink: '#170F2C',
    // หนวดต้องสว่างจัด เป็นเส้นบางบนตัวเข้ม ถ้าจางกว่านี้จะหายไปเลย
    whisker: 'rgba(240,234,255,.95)',
    // เม็ดที่โปรยตอนใช้ความสามารถเป็นม่วงเรืองแสง ให้เห็นบนฉากเข้มได้
    rain: ['#9B7BF0', '#DCC9FF', '#432E7A'],
    stripes: false,
    blush: false,
    // แมวที่ไม่มีช่อง cost ถือว่าได้ฟรีตั้งแต่แรก
    cost: 10000,
  },
  {
    id: 'tabby',
    name: 'ปลาสลิด',
    note: 'ลายสวยประจำบ้าน',
    // เดิมเป็นน้ำตาลอมเทา #9E8A6A ซึ่งเป็นสีที่ "ถูกต้องตามแมวจริง" แต่หม่นเกิน
    // สำหรับเกมสีจัด เปลี่ยนเป็นน้ำผึ้งอมทอง ยังอ่านเป็นลายปลาสลิดเพราะจุดเด่น
    // ของมันคือลายเข้มพาดตัวกับตาเขียว ไม่ใช่ความหม่นของขน
    cat: '#D08A45',
    line: '#5A3212',   // น้ำตาลไหม้ สีเดียวกับลาย
    dark: '#8A5220',
    cream: '#FFEBC6',
    pink: '#F09AA8',
    // ── ทำไม ink เป็นเขียวเข้มไม่ใช่ดำ ──
    // ช่องนี้ถูกใช้วาดทั้งดวงตาและเส้นปาก แมวปลาสลิดเด่นที่ตาสีเขียว
    // จึงเลือกเขียวที่เข้มพอจะอ่านเป็นเส้นปากได้ตามปกติ แต่ยังเห็นเป็นประกาย
    // เขียวตอนซูมดูหน้า รอบนี้ดันให้เขียวขึ้นอีกขั้นเพราะของเดิมออกไปทางน้ำตาล
    eye: '#24401C',   // สีดวงตา
    ink: '#3A2A16',   // ปากเป็นน้ำตาลเข้ม ไม่ใช่เขียวตามสีตา
    whisker: 'rgba(255,250,238,.9)',
    rain: ['#E0A05C', '#FFE3B4', '#8A5220'],
    stripes: true,
    blush: false,
    cost: 11000,
  },
  {
    id: 'siamese',
    name: 'วิเชียรมาศ',
    note: 'แมวไทยตาสีฟ้า',
    // ── แมว "แต้ม" ตัวเดียวในเกม ──
    // วิเชียรมาศจริงคือตัวครีมอ่อนกับปลายขนสีเข้มสี่จุด: หู หน้ากากรอบจมูก ขา หาง
    // เดิมเอนจินวาดให้แค่ขา (เพราะขาใช้สี dark อยู่แล้วทุกสกิน) ที่เหลือเลยเป็นครีมล้วน
    // จนอ่านไม่ออกว่าเป็นวิเชียรมาศ — ตอนนี้เปิดสวิตช์ points เพื่อให้หู หาง และ
    // หน้ากากถูกวาดด้วยสีปลายขนด้วย
    //
    // dark เปลี่ยนจากน้ำตาลอมม่วง #4A3552 เป็นน้ำตาลอมเทาอุ่น ซึ่งเป็นสีปลายขนจริง
    // ของสายพันธุ์นี้ ม่วงเข้าชุดกับโทนเกมก็จริงแต่มันไม่ใช่สีแมว
    cat: '#F4E0BE',
    line: '#6B4A38',   // โกโก้ เป็นสีเดียวกับปลายขนของสายพันธุ์นี้พอดี
    dark: '#5C4941',
    points: true,
    // จมูกวิเชียรมาศเป็นหนังสีน้ำตาลเข้ม ไม่ใช่ชมพูอย่างแมวสีอื่น
    // แยกเป็นช่องของตัวเองแทนการใช้ dark ซ้ำ เพราะต้องเข้มกว่าแต้มอีกขั้น
    // ไม่งั้นจมูกจะจมหายไปในหน้ากากที่เป็นสีเดียวกัน
    nose: '#46352C',
    cream: '#FFF6E8',
    pink: '#F5A6BC',
    // ── ตาสีฟ้า ──
    // ช่องนี้วาดทั้งดวงตาและเส้นปาก จึงเลือกน้ำเงินที่ยังอ่านเป็นเส้นได้ปกติ
    // แต่พอเป็นวงกลมใหญ่อย่างดวงตาจะเห็นเป็นสีฟ้าชัด ซึ่งเป็นจุดเด่นของสายพันธุ์นี้
    // สดขึ้นกว่าเดิมหนึ่งขั้น ของเดิม #1B3A63 เข้มจนตาเกือบเป็นสีดำ
    eye: '#1F5AA8',   // สีดวงตา
    ink: '#4A382F',   // ปากเป็นน้ำตาลตามสีแต้ม ไม่ใช่ฟ้าตามสีตา
    whisker: 'rgba(255,253,248,.92)',
    rain: ['#E4CCA4', '#FFF6E8', '#4A3552'],
    stripes: false,
    blush: false,
    cost: 13000,
  },
  {
    id: 'grey',
    name: 'เทาหมอก',
    note: 'ขนเทานุ่มเหมือนหมอก',
    // เดิมเป็นเทาอมฟ้าจาง ๆ ซึ่งยังเป็นสีกลาง ๆ ที่ไม่มีบุคลิก
    // ดันไปทางน้ำเงินม่วงให้ชัดขึ้น กลายเป็น "เทาหมอกยามเช้า" ที่มีสีในตัวจริง ๆ
    // และแยกออกจากขาวมุกได้ทันทีแม้เห็นแค่เงาร่าง
    cat: '#93A5D2',
    line: '#37436B',   // น้ำเงินเข้ม
    dark: '#5C6A9B',
    cream: '#E9EEFC',
    pink: '#FF9EC0',
    eye: '#202742',   // สีดวงตา
    ink: '#202742',
    whisker: 'rgba(252,253,255,.94)',
    rain: ['#AEBCE4', '#E9EEFC', '#5C6A9B'],
    stripes: false,
    blush: true,
    cost: 14000,
  },
];

// ── น้องที่ผู้เล่นระบายสีเอง ────────────────────────────────
//
// ไม่ได้เขียนสีตายตัวไว้เหมือนหกตัวข้างบน แต่ใช้ getter ดึงจากจานสีที่ระบายไว้
// ทุกครั้งที่มีคนอ่านช่องนั้น ทำแบบนี้เพื่อให้ "ระบายปุ๊บเห็นผลปั๊บทุกที่ในเกม"
// ทั้งการ์ดในเมนู ไอคอนล็อบบี้ และตัวจริงตอนวิ่ง โดยไม่ต้องไล่สั่งวาดใหม่ทีละหน้า
//
// ไม่มีช่อง cost = ได้ฟรี ไม่เกี่ยวกับกาช่า ตรงตามที่ตั้งใจว่านี่คือการระบายสี
// ไม่ใช่การได้ตัวละครใหม่
const MINE = { id: CUSTOM_ID, name: 'น้องของเรา', note: 'ระบายสีเอง' };
for (const k of ['cat', 'dark', 'cream', 'pink', 'nose', 'eye', 'ink',
                 'line', 'whisker', 'rain', 'stripes', 'blush', 'points']) {
  Object.defineProperty(MINE, k, { get: () => customSkin()[k], enumerable: true });
}
SKINS.push(MINE);

// ── การปลดล็อก ─────────────────────────────────────────────
//
// แมวที่ไม่มี cost = ได้ฟรี ไม่ต้องบันทึกอะไร
// แมวที่มี cost = ต้องอยู่ในรายชื่อที่ซื้อแล้วถึงจะใช้ได้

export function ownsSkin(id) {
  const s = skinById(id);
  return !s.cost || loadSkinsOwned().includes(s.id);
}

/**
 * บันทึกว่าซื้อแมวตัวนี้แล้ว
 *
 * ไม่หักทองในนี้ — เรื่องเงินเป็นของ gacha.js ที่เดียว
 * ถ้าหักสองที่จะเกิดกรณีหักแล้วแต่บันทึกไม่ผ่าน (หรือกลับกัน) ซึ่งแก้ทีหลังยากมาก
 */
export function unlockSkin(id) {
  const list = loadSkinsOwned();
  if (!list.includes(id)) {
    list.push(id);
    saveSkinsOwned(list);
  }
}

export function skinById(id) {
  return SKINS.find((s) => s.id === id) || SKINS[0];
}

// ถ้าค่าที่บันทึกไว้เป็นตัวที่ยังไม่ได้ซื้อ (ล้างข้อมูลการซื้อทิ้ง หรือแก้ค่าในเครื่องเอง)
// ต้องดีดกลับตัวเริ่มต้น ไม่งั้นผู้เล่นจะใช้ตัวที่ยังไม่ได้จ่ายเงินได้
let current = skinById(loadSkin());
if (current.cost && !ownsSkin(current.id)) current = SKINS[0];

/**
 * คืนสกินที่ผนวก "ชุดที่ใส่อยู่" เข้าไปแล้ว
 *
 * ทุกจุดที่วาดแมวรับ s ตัวเดียวกันนี้อยู่แล้ว การแนบชุดมากับ s
 * จึงทำให้ไม่ต้องแก้ signature ของฟังก์ชันวาดสักตัว
 *
 * จำผลไว้เพราะ getSkin() ถูกเรียกหลายครั้งต่อเฟรม ถ้าสร้างอ็อบเจกต์ใหม่ทุกครั้ง
 * จะกลายเป็นขยะให้ GC เก็บ 200+ ก้อนต่อวินาทีโดยไม่จำเป็น
 */
let view = null;

export function getSkin() {
  const outfit = getOutfit();
  // น้องที่ระบายเองเปลี่ยนสีได้ตลอดเวลา เทียบด้วย id อย่างเดียวไม่พอ
  // ต้องเทียบตัวอ็อบเจกต์สกินด้วย ซึ่ง customSkin() คืนก้อนเดิมจนกว่าจะระบายใหม่
  if (current.id === CUSTOM_ID) {
    const mine = customSkin();
    if (!view || view.id !== CUSTOM_ID || view.outfit !== outfit || view.src !== mine) {
      view = { ...mine, outfit, src: mine };
    }
    return view;
  }
  if (!view || view.id !== current.id || view.outfit !== outfit) {
    view = { ...current, outfit };
  }
  return view;
}

export function setSkin(id) {
  // ด่านสุดท้ายกันการเปลี่ยนไปตัวที่ยังไม่ได้ซื้อ ฝั่ง UI กันไว้แล้วชั้นหนึ่ง
  // แต่ setSkin ถูกเรียกได้จากหลายที่ จึงต้องกันที่ตัวมันเองด้วย
  if (!ownsSkin(id)) return current;
  current = skinById(id);
  saveSkin(current.id);
  return current;
}
